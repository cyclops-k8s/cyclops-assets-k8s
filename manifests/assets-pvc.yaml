apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assets
  namespace: cyclops-assets
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: cyclops-fs
  volumeMode: Filesystem
